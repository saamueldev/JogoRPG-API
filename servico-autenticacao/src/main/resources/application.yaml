spring:
  application:
    name: servico-autenticacao

  datasource:
    url: jdbc:postgresql://localhost:5432/db_autenticacao
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080