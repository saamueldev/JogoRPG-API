spring:
  application:
    name: servico-combate

  datasource:
    url: jdbc:postgresql://localhost:5432/db_combate
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

server:
  port: 8083